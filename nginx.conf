server {
    listen 80;
    server_name localhost;

    # 前端靜態文件
    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    # 代理後端 API
    location /api/ {
        proxy_pass http://backend:5000/;
    }
}